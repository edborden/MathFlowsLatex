machine:
  services:
    - docker

database:
  override:
    - ls
test:
  override:
    - ls
deployment:
  production:
    branch: master
    commands:
      - rm Gemfile.lock
      - heroku plugins:install heroku-docker
      - heroku docker:release --app mathflows-latex